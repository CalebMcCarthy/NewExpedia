<h1>
EVENTS MAP
</h1>
<% @events.each do |event|%>
  <h2> <a href= "event/<%=event.id %>"> <%= event.name %> </a> </h2>
<% end %>
<link rel="stylesheet" href="/assets/login.css" rel="stylesheet">
<button> <a href="/newevent">Create a New Event</a> </button>
<button> <a href="/users/<%= session[:user_id] %>">Profile Page</a> </button>
<button> <a href="users/<%= session[:user_id] %>/edit">Edit Profile</a> </button>


<div id="map" style="height: 1000px; width: 1000px; saturation: -100%;"></div>

<script>
      function initMap() {
        var uluru = {lat: -25.363, lng: 131.044};
        var map = new google.maps.Map(document.getElementById('map'), {
          zoom: 4,
          center: uluru
        });
        var marker = new google.maps.Marker({
          position: uluru,
          map: map
        });
      }
    </script>


<script async defer
    src="https://maps.googleapis.com/maps/api/js?key=<%= ENV[Rails.application.secrets.google_maps_key] %>&callback=initMap">
    </script>
